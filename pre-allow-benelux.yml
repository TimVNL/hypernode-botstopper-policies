# Allow traffic from Benelux countries without applying a challenge.
- name: allow-benelux-without-challenge
  action: ALLOW
  expression:
    all:
      - '"X-Country-Code" in headers'
      - 'headers["X-Country-Code"] in ["NL", "BE", "LU"]'